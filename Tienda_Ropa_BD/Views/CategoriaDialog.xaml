<Window x:Class="TiendaRopaPOS.Views.CategoriaDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nueva Categoría" Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        MinHeight="260" MinWidth="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Nueva Categoría" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <TextBlock Grid.Row="1" Text="Nombre:" Margin="0,0,0,5"/>
        <TextBox x:Name="TxtNombre" Grid.Row="2" 
                 Style="{StaticResource ModernTextBox}" Margin="0,0,0,15"/>
        
        <TextBlock Grid.Row="3" Text="Detalles:" Margin="0,0,0,5"/>
        <TextBox x:Name="TxtDetalles" Grid.Row="4" 
                 Style="{StaticResource ModernTextBox}" 
                 TextWrapping="Wrap" AcceptsReturn="True" 
                 Height="80" Margin="0,0,0,20"/>
        
        <StackPanel Grid.Row="5" Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Guardar" Style="{StaticResource SuccessButton}" 
                    Click="BtnGuardar_Click" Width="100" Margin="0,0,10,0"/>
            <Button Content="Cancelar" Style="{StaticResource DangerButton}" 
                    Click="BtnCancelar_Click" Width="100"/>
        </StackPanel>
    </Grid>
</Window>