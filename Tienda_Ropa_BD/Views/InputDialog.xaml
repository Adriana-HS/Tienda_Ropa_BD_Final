<Window x:Class="TiendaRopaPOS.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="150" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="150" MinWidth="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock x:Name="TxtPrompt" Grid.Row="0" 
                  TextWrapping="Wrap" Margin="0,0,0,10"/>
        
        <TextBox x:Name="TxtInput" Grid.Row="1" 
                Style="{StaticResource ModernTextBox}" 
                KeyDown="TxtInput_KeyDown" Margin="0,0,0,20"/>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                   HorizontalAlignment="Right">
            <Button Content="Aceptar" Style="{StaticResource SuccessButton}" 
                   Click="BtnAceptar_Click" Width="100" Margin="0,0,10,0"/>
            <Button Content="Cancelar" Style="{StaticResource DangerButton}" 
                   Click="BtnCancelar_Click" Width="100"/>
        </StackPanel>
    </Grid>
</Window>

