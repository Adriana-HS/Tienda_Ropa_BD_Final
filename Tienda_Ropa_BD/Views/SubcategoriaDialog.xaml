<Window x:Class="TiendaRopaPOS.Views.SubcategoriaDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nueva Subcategoría" Height="350" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        MinHeight="320" MinWidth="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Nueva Subcategoría" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <TextBlock Grid.Row="1" Text="Nombre:" Margin="0,0,0,5"/>
        <TextBox x:Name="TxtNombre" Grid.Row="2" 
                 Style="{StaticResource ModernTextBox}" Margin="0,0,0,15"/>
        
        <TextBlock Grid.Row="3" Text="Categoría:" Margin="0,0,0,5"/>
        <ComboBox x:Name="CmbCategoria" Grid.Row="4" 
                 DisplayMemberPath="Nombre"
                 SelectedValuePath="IdCategoria"
                 Style="{StaticResource ModernComboBox}" 
                 Margin="0,0,0,15"/>
        
        <TextBlock Grid.Row="5" Text="Detalles:" Margin="0,0,0,5"/>
        <TextBox x:Name="TxtDetalles" Grid.Row="6" 
                 Style="{StaticResource ModernTextBox}" 
                 TextWrapping="Wrap" AcceptsReturn="True" 
                 Height="80" Margin="0,0,0,20"/>
        
        <StackPanel Grid.Row="7" Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Guardar" Style="{StaticResource SuccessButton}" 
                    Click="BtnGuardar_Click" Width="100" Margin="0,0,10,0"/>
            <Button Content="Cancelar" Style="{StaticResource DangerButton}" 
                    Click="BtnCancelar_Click" Width="100"/>
        </StackPanel>
    </Grid>
</Window>