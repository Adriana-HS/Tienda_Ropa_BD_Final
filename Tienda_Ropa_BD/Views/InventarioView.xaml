<UserControl x:Class="TiendaRopaPOS.Views.InventarioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="MÓDULO DE INVENTARIO" 
                  FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <!-- Action Buttons Top -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" 
                   Margin="0,0,0,20">
            <Button x:Name="BtnNuevoProducto" Content="➕ Nuevo Producto" 
                   Style="{StaticResource SuccessButton}"
                   Click="BtnNuevoProducto_Click" 
                   Width="150" Margin="0,0,10,0"/>
            <Button x:Name="BtnNuevaCategoria" Content="📁 Nueva Categoría" 
                   Style="{StaticResource PrimaryButton}"
                   Click="BtnNuevaCategoria_Click" 
                   Width="150" Margin="0,0,10,0"/>
            <Button x:Name="BtnNuevaSubcategoria" Content="📂 Nueva Subcategoría" 
                   Style="{StaticResource PrimaryButton}"
                   Click="BtnNuevaSubcategoria_Click" 
                   Width="150"/>
        </StackPanel>
        
        <DataGrid x:Name="DgProductos" Grid.Row="2" 
                 Style="{StaticResource ModernDataGrid}"
                 MouseDoubleClick="DgProductos_MouseDoubleClick"
                 SelectionChanged="DgProductos_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdProducto}" Width="60"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="200"/>
                <DataGridTextColumn Header="Color" Binding="{Binding Color}" Width="100"/>
                <DataGridTextColumn Header="Talla" Binding="{Binding Talla}" Width="80"/>
                <DataGridTextColumn Header="Categoría" Binding="{Binding Categoria}" Width="120"/>
                <DataGridTextColumn Header="Subcategoría" Binding="{Binding Subcategoria}" Width="120"/>
                <DataGridTextColumn Header="Precio Base" Binding="{Binding PrecioBase, StringFormat=C2}" Width="100"/>
                <DataGridTextColumn Header="Precio Venta" Binding="{Binding PrecioVenta, StringFormat=C2}" Width="100"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding StockActual}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                   VerticalAlignment="Bottom" HorizontalAlignment="Right" 
                   Margin="0,0,0,10">
            <Button x:Name="BtnEditarPrecio" Content="💰 Editar Precio" 
                   Style="{StaticResource PrimaryButton}"
                   Click="BtnEditarPrecio_Click" 
                   Margin="0,0,10,0" Width="150" IsEnabled="False"/>
            <Button x:Name="BtnAjustarStock" Content="📦 Ajustar Stock" 
                   Style="{StaticResource SuccessButton}"
                   Click="BtnAjustarStock_Click" 
                   Margin="0,0,10,0" Width="150" IsEnabled="False"/>
            <Button x:Name="BtnVerDetalle" Content="👁️ Ver Detalle" 
                   Style="{StaticResource PrimaryButton}"
                   Click="BtnVerDetalle_Click" 
                   Width="150" IsEnabled="False"/>
        </StackPanel>
    </Grid>
</UserControl>

